{% extends 'back/base.html.twig' %}

{% block content %}
    <center>
    <h1>Les Dommaines des livres</h1>

    <table border="3" bgcolor="blue" size="1000">
        <tbody>

            <tr>
                <th>Libelle</th>
                <td>{{ dommaine.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dommaine_index') }}">retour</a>
        </li>
        <li>
            <a href="{{ path('dommaine_edit', { 'id': dommaine.id }) }}">modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
    </center>
{% endblock %}

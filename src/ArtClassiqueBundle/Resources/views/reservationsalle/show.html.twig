{% extends 'base.html.twig' %}

{% block body %}
    <h1>Reservationsalle</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservationSalle.id }}</td>
            </tr>
            <tr>
                <th>Typespectacle</th>
                <td>{{ reservationSalle.typeSpectacle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('salle_reservation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('salle_reservation_edit', { 'id': reservationSalle.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
